var string="hello";
var l=string.length;//calculate length of string
console.log(string[l-1]);//shows last chatacter of sting
//shows multiline output
const person = {
    name: "Zodiac Hasbro",
    age: 56
  };
  
  const greeting = `Hello, my name is ${person.name}!
  I am ${person.age} years old.`;
  
  console.log(greeting);